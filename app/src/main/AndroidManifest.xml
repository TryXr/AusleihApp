<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QR_SCANNER"
        tools:targetApi="31">
        <activity
            android:name=".BearbeitenView"
            android:exported="false" />
        <activity
            android:name=".NewObjectView"
            android:exported="false" />
        <activity
            android:name=".AnnahmeView"
            android:exported="false" />
        <activity
            android:name=".AusgabeView"
            android:exported="false" />
        <activity
            android:name=".StudentView"
            android:exported="false" />
        <activity
            android:name=".LeherView"
            android:exported="false" />
        <activity
            android:name=".login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ScannerKlasse"
            android:exported="true" />
    </application>

</manifest>